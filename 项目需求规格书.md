# CTHC 公寓租赁管理系统

## 企业级项目需求规格书

---

**文档版本**: V1.0  
**编制日期**: 2026年3月20日  
**项目名称**: CTHC（产投华创房产）公寓租赁管理系统  
**文档类型**: 产品需求规格说明书（PRD）

---

## 目录

1. [项目概述](#1-项目概述)
2. [业务背景与目标](#2-业务背景与目标)
3. [系统架构](#3-系统架构)
4. [功能需求](#4-功能需求)
5. [非功能需求](#5-非功能需求)
6. [数据模型](#6-数据模型)
7. [业务流程](#7-业务流程)
8. [接口规范](#8-接口规范)
9. [安全需求](#9-安全需求)
10. [部署与运维](#10-部署与运维)

---

## 1. 项目概述

### 1.1 项目简介

CTHC 公寓租赁管理系统是一套面向房产租赁企业的全栈企业级应用，旨在实现公寓租赁业务的全生命周期数字化管理。系统涵盖房源管理、订单处理、合同管理、账单管理、财务统计等核心业务模块，为租赁企业提供完整的业务闭环解决方案。

### 1.2 项目定位

- **业务领域**: 长租公寓租赁管理
- **目标用户**: 房产租赁企业、门店店长、租客
- **系统形态**: Web管理后台 + H5移动端 + RESTful API服务
- **部署模式**: 支持私有化部署

### 1.3 核心价值

1. **业务流程标准化**: 规范从房源发布到合同终止的全流程
2. **数据可视化**: 提供实时业务数据与趋势分析
3. **多端协同**: 管理端与租客端无缝衔接
4. **合规性保障**: 支持住建局合同报备等监管要求

---

## 2. 业务背景与目标

### 2.1 业务背景

随着长租公寓市场的快速发展，传统的手工管理方式已无法满足业务需求：

- 房源信息分散，难以统一管理
- 合同流程复杂，容易出错
- 账单计算繁琐，催缴效率低
- 缺乏数据支撑，决策困难

### 2.2 业务目标

| 目标维度 | 具体目标 |
|---------|---------|
| 效率提升 | 合同处理时间缩短50% |
| 成本控制 | 人工核算成本降低60% |
| 合规保障 | 100%合同报备覆盖 |
| 数据透明 | 实时业务数据可视化 |

### 2.3 用户角色

| 角色 | 职责描述 | 使用端 |
|-----|---------|-------|
| 系统管理员 | 系统配置、用户管理、数据查看 | 管理后台 |
| 店长 | 门店房源管理、订单审核、合同管理 | 管理后台 |
| 租客 | 房源浏览、下单、合同签署、账单支付 | H5移动端 |

---

## 3. 系统架构

### 3.1 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                        前端层                                │
│  ┌─────────────────────┐    ┌─────────────────────────────┐ │
│  │   Admin 管理后台     │    │   Mobile H5 租客端          │ │
│  │   (React + Vite)    │    │   (React + Vite)            │ │
│  └──────────┬──────────┘    └──────────────┬──────────────┘ │
│             │                              │                │
│             └──────────────┬───────────────┘                │
│                            │                                │
│                       HTTP API                              │
└────────────────────────────┼────────────────────────────────┘
                             │
┌────────────────────────────┼────────────────────────────────┐
│                        后端层                                │
│                   ┌────────┴────────┐                       │
│                   │     Server      │                       │
│                   │   (Express)     │                       │
│                   └────────┬────────┘                       │
│                            │                                │
│                      Prisma ORM                             │
│                            │                                │
│                   ┌────────┴────────┐                       │
│                   │     SQLite      │                       │
│                   │   (开发环境)     │                       │
│                   └─────────────────┘                       │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 技术栈

| 层级 | 技术选型 | 版本 |
|-----|---------|------|
| 前端框架 | React | 19.2.0 |
| 构建工具 | Vite | 7.3.1 |
| 路由 | React Router | 6.28.0 |
| 图表 | Recharts | 2.15.0 |
| 后端框架 | Express | 5.1.0 |
| ORM | Prisma | 6.16.0 |
| 数据库 | SQLite | - |
| 认证 | JWT | 9.0.2 |
| 包管理 | npm workspaces | - |

### 3.3 项目结构

```
cthc/
├── package.json              # 根配置，npm workspaces
├── README.md
├── packages/
│   ├── admin/               # 后台管理系统
│   │   ├── src/
│   │   │   ├── pages/      # 业务页面
│   │   │   ├── layout/     # 布局组件
│   │   │   └── components/ # 公共组件
│   │   └── package.json
│   ├── mobile/              # H5移动端
│   │   ├── src/
│   │   │   └── pages/      # 业务页面
│   │   └── package.json
│   └── server/              # 后端服务
│       ├── src/
│       │   ├── routes.ts   # API路由
│       │   ├── services/   # 业务服务
│       │   └── schedulers.ts # 定时任务
│       └── prisma/
│           ├── schema.prisma # 数据模型
│           └── seed.ts      # 种子数据
```

---

## 4. 功能需求

### 4.1 功能架构图

```
┌─────────────────────────────────────────────────────────────┐
│                      CTHC 功能架构                           │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │   房源管理   │  │   订单管理   │  │   合同管理   │         │
│  │  (Houses)   │  │  (Orders)   │  │ (Contracts) │         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │   账单管理   │  │   财务管理   │  │   系统管理   │         │
│  │  (Bills)    │  │   (Finance) │  │   (System)  │         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
│  ┌─────────────┐  ┌─────────────┐                           │
│  │   逾期管理   │  │   催租管理   │                           │
│  │  (Overdue)  │  │ (Reminders) │                           │
│  └─────────────┘  └─────────────┘                           │
└─────────────────────────────────────────────────────────────┘
```

### 4.2 管理后台功能（Admin）

#### 4.2.1 首页仪表盘

| 功能模块 | 功能描述 | 优先级 |
|---------|---------|-------|
| 数据统计卡片 | 展示房源、订单、合同、账单、逾期账单数量 | P0 |
| 业务趋势图 | 按时间维度展示业务增长趋势（折线图） | P0 |
| 出租率/收缴率 | 展示出租率、空置率、收缴率等关键指标 | P0 |
| 时间筛选 | 支持昨日、本周、上周、本月、自定义时间范围 | P1 |

#### 4.2.2 房源管理

| 功能模块 | 功能描述 | 优先级 |
|---------|---------|-------|
| 房源列表 | 展示所有房源，支持分页、筛选、排序 | P0 |
| 房源详情 | 查看房源详细信息（基本信息、图片、位置等） | P0 |
| 房源编辑 | 编辑房源信息（租金、押金、图片、地址等） | P0 |
| 发布/下架 | 控制房源在H5端的展示状态 | P0 |
| 地图信息 | 配置房源地图坐标、周边配套信息 | P1 |

#### 4.2.3 订单管理

| 功能模块 | 功能描述 | 优先级 |
|---------|---------|-------|
| 订单列表 | 展示所有订单，支持按状态筛选 | P0 |
| 订单审核 | 审核租客提交的订单（通过/拒绝） | P0 |
| 生成合同 | 审核通过后自动生成合同 | P0 |
| 订单详情 | 查看订单完整信息 | P0 |

#### 4.2.4 合同管理

| 功能模块 | 功能描述 | 优先级 |
|---------|---------|-------|
| 合同列表 | 展示所有合同，支持多维度筛选 | P0 |
| 合同详情 | 查看合同完整信息、租客信息、账单列表 | P0 |
| 合同配置 | 配置合同条款、备注、附件 | P0 |
| 合同盖章 | 模拟合同盖章操作 | P0 |
| 合同续签 | 为到期合同创建续签合同 | P0 |
| 合同换房 | 为租客更换房源并生成新合同 | P0 |
| 合同终止 | 终止生效中的合同 | P0 |
| 附件管理 | 上传/下载合同相关附件 | P1 |
| 住建报备 | 查看合同报备状态，手动触发报备 | P1 |

#### 4.2.5 账单管理

| 功能模块 | 功能描述 | 优先级 |
|---------|---------|-------|
| 账单列表 | 展示所有账单，支持按状态、账期筛选 | P0 |
| 账单导入 | 通过Excel导入水电等变动费用 | P0 |
| 账单核销 | 线下收款后核销账单 | P0 |
| 账单调整 | 修改账单金额（需记录变更日志） | P1 |
| 账期锁定 | 锁定已结算账期，防止数据变更 | P1 |

#### 4.2.6 逾期管理

| 功能模块 | 功能描述 | 优先级 |
|---------|---------|-------|
| 逾期列表 | 展示所有逾期账单 | P0 |
| 滞纳金计算 | 自动计算逾期滞纳金（日0.1%） | P0 |
| 催租记录 | 查看催租短信发送记录 | P1 |

#### 4.2.7 系统管理

| 功能模块 | 功能描述 | 优先级 |
|---------|---------|-------|
| 部门管理 | 管理部门组织架构 | P1 |
| 角色管理 | 管理系统角色及权限 | P1 |
| 用户管理 | 管理后台用户账号 | P1 |
| 个人中心 | 查看/修改个人信息 | P1 |

### 4.3 H5移动端功能（Mobile）

#### 4.3.1 房源浏览

| 功能模块 | 功能描述 | 优先级 |
|---------|---------|-------|
| 房源列表 | 浏览可租房源列表 | P0 |
| 房源筛选 | 按区域、户型、租金、面积筛选 | P0 |
| 房源详情 | 查看房源详细信息、图片轮播 | P0 |
| 地图找房 | 地图模式查看房源位置 | P1 |

#### 4.3.2 订单流程

| 功能模块 | 功能描述 | 优先级 |
|---------|---------|-------|
| 创建订单 | 填写租房申请信息 | P0 |
| 订单状态 | 查看订单审核状态 | P0 |
| 订单列表 | 查看历史订单记录 | P0 |

#### 4.3.3 合同管理

| 功能模块 | 功能描述 | 优先级 |
|---------|---------|-------|
| 合同查看 | 查看合同详细内容 | P0 |
| 合同确认 | 确认合同条款 | P0 |
| 申请修改 | 申请修改合同配置 | P1 |

#### 4.3.4 账单管理

| 功能模块 | 功能描述 | 优先级 |
|---------|---------|-------|
| 账单列表 | 查看待缴/已缴账单 | P0 |
| 账单详情 | 查看账单明细 | P0 |
| 在线支付 | 支付押金和租金（演示模式） | P0 |

---

## 5. 非功能需求

### 5.1 性能需求

| 指标 | 要求 |
|-----|------|
| 页面加载时间 | < 3秒 |
| API响应时间 | < 500ms（P95） |
| 并发用户支持 | 100+ |
| 数据库查询 | 简单查询 < 100ms |

### 5.2 可用性需求

| 指标 | 要求 |
|-----|------|
| 系统可用性 | 99.9% |
| 数据备份 | 每日自动备份 |
| 故障恢复 | < 30分钟 |

### 5.3 兼容性需求

| 平台 | 支持版本 |
|-----|---------|
| 浏览器 | Chrome 90+, Firefox 88+, Safari 14+ |
| 移动端 | iOS 12+, Android 8+ |
| 屏幕适配 | 支持PC、平板、手机多端适配 |

### 5.4 可维护性需求

- 代码需遵循 TypeScript 类型规范
- 关键业务逻辑需有单元测试覆盖
- API 接口需有完整文档
- 数据库变更需通过迁移脚本管理

---

## 6. 数据模型

### 6.1 实体关系图

```
┌──────────┐     ┌──────────┐     ┌──────────┐
│  Store   │────<│Apartment │────<│  House   │
│  (门店)   │     │  (公寓)  │     │  (房源)  │
└──────────┘     └──────────┘     └────┬─────┘
                                       │
                              ┌────────┼────────┐
                              ▼        ▼        ▼
                         ┌────────┐ ┌──────┐ ┌────────┐
                         │  Order │ │Tenant│ │Contract│
                         │ (订单)  │ │(租客)│ │(合同)  │
                         └───┬────┘ └──────┘ └───┬────┘
                             │                   │
                             │         ┌────────┼────────┐
                             │         ▼        ▼        ▼
                             │    ┌────────┐ ┌──────┐ ┌────────┐
                             └───>│Payment │ │ Bill │ │ Refund │
                                  │(支付)  │ │(账单)│ │(退款)  │
                                  └────────┘ └──┬───┘ └────────┘
                                                │
                                          ┌─────┴─────┐
                                          ▼           ▼
                                     ┌────────┐  ┌──────────┐
                                     │BillItem│  │ Arrears  │
                                     │(账单项)│  │(逾期记录)│
                                     └────────┘  └──────────┘
```

### 6.2 核心实体定义

#### 6.2.1 房源（House）

| 字段 | 类型 | 说明 |
|-----|------|------|
| id | String | 唯一标识（CUID） |
| houseNo | String | 房号 |
| houseType | String | 户型（开间/一室一厅等） |
| area | Float | 面积（平方米） |
| rentMonthly | Int | 月租金（分） |
| deposit | Int | 押金（分） |
| status | Enum | 状态：空置/预约/已订/已签/已终止 |
| isPublished | Boolean | 是否上架 |
| houseImagesJson | String | 房源图片JSON数组 |
| address | String | 详细地址 |
| geoLat/geoLng | Float | 地图坐标 |

#### 6.2.2 订单（Order）

| 字段 | 类型 | 说明 |
|-----|------|------|
| id | String | 唯一标识 |
| houseId | String | 关联房源 |
| tenantId | String | 关联租客 |
| leaseMonths | Int | 租期（月） |
| moveInDate | DateTime | 入住日期 |
| status | Enum | 状态：待审核/已通过/已拒绝/已取消 |
| reviewReason | String | 审核意见 |

#### 6.2.3 合同（Contract）

| 字段 | 类型 | 说明 |
|-----|------|------|
| id | String | 唯一标识 |
| contractNo | String | 合同编号（唯一） |
| houseId | String | 关联房源 |
| tenantId | String | 关联租客 |
| orderId | String | 关联订单 |
| status | Enum | 状态：待签字/待盖章/待支付/已生效/已作废/已终止 |
| startDate | DateTime | 合同开始日期 |
| endDate | DateTime | 合同结束日期 |
| rentMonthly | Int | 月租金 |
| deposit | Int | 押金 |
| depositMultiple | Float | 押金倍数 |
| penaltyFormula | String | 滞纳金计算公式 |
| renewedFromId | String | 续签来源合同ID |
| changeHouseFromId | String | 换房来源合同ID |

#### 6.2.4 账单（Bill）

| 字段 | 类型 | 说明 |
|-----|------|------|
| id | String | 唯一标识 |
| contractId | String | 关联合同 |
| period | String | 账期（如 2026-03） |
| dueDate | DateTime | 到期日 |
| totalAmount | Int | 总金额（分） |
| status | Enum | 状态：未支付/已支付/已逾期 |
| kind | Enum | 类型：基础账单/补缴账单 |
| paidAt | DateTime | 支付时间 |
| offlineVerifiedAt | DateTime | 线下核销时间 |

### 6.3 枚举类型定义

| 枚举名 | 值 | 说明 |
|-------|-----|------|
| HouseStatus | VACANT, RESERVED, ORDERED, SIGNED, TERMINATED | 房源状态 |
| OrderStatus | PENDING_REVIEW, APPROVED, REJECTED, CANCELLED | 订单状态 |
| ContractStatus | WAIT_TENANT_SIGN, WAIT_STAMP, PENDING_PAYMENT, ACTIVE, VOID, TERMINATED | 合同状态 |
| BillStatus | UNPAID, PAID, OVERDUE | 账单状态 |
| BillKind | BASE, ADJUSTMENT | 账单类型 |
| AdminRoleCode | SYSTEM_ADMIN, STORE_MANAGER | 管理员角色 |

---

## 7. 业务流程

### 7.1 租房主流程

```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│ 浏览房源  │───>│ 提交订单  │───>│ 订单审核  │───>│ 生成合同  │───>│ 租客确认  │
└──────────┘    └──────────┘    └──────────┘    └──────────┘    └────┬─────┘
                                                                     │
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌───────────────────┘
│ 住建报备  │<───│ 合同生效  │<───│ 完成支付  │<───┘
└──────────┘    └──────────┘    └──────────┘
```

### 7.2 合同变更流程

#### 7.2.1 续签流程

```
原合同(ACTIVE) ──> 创建续签合同 ──> 新合同(ACTIVE)
                      │
                      └── 原合同状态变更为 TERMINATED
                      └── 原合同.renewedTo 指向新合同
                      └── 新合同.renewedFrom 指向原合同
```

#### 7.2.2 换房流程

```
旧合同(ACTIVE) ──> 终止旧合同 ──> 创建新房合同 ──> 新合同(ACTIVE)
                      │
                      └── 旧合同状态变更为 TERMINATED
                      └── 旧房源状态恢复为 VACANT
                      └── 新合同.changeHouseFrom 指向旧合同
```

### 7.3 账单处理流程

```
合同生效
    │
    ▼
预生成基础账单（BASE）
    │
    ▼
管理员导入实际费用 ──> 生成补缴账单（ADJUSTMENT）
    │
    ▼
租客支付 / 线下核销
    │
    ▼
账单状态变更为 PAID
```

### 7.4 定时任务

| 任务名称 | 执行频率 | 功能描述 |
|---------|---------|---------|
| 合同过期处理 | 每10分钟 | 处理24小时未支付的合同，作废并释放房源 |
| 住建报备 | 每天凌晨2点 | 对生效但未报备的合同执行报备 |

---

## 8. 接口规范

### 8.1 接口设计原则

- 遵循 RESTful API 设计规范
- 使用 JSON 作为数据交换格式
- 统一返回格式：`{ ok: boolean, data?: any, error?: string }`
- 认证方式：JWT Bearer Token

### 8.2 核心接口列表

#### 8.2.1 公共接口（H5）

| 方法 | 路径 | 功能 |
|-----|------|------|
| GET | /api/houses | 获取房源列表 |
| GET | /api/houses/:id | 获取房源详情 |
| POST | /api/orders | 创建订单 |
| GET | /api/contracts | 获取租客合同列表 |
| GET | /api/contracts/:id | 获取合同详情 |
| POST | /api/contracts/:id/confirm | 确认合同 |

#### 8.2.2 管理后台接口

| 方法 | 路径 | 功能 |
|-----|------|------|
| POST | /api/admin/login | 管理员登录 |
| GET | /api/admin/me | 获取当前管理员信息 |
| GET | /api/admin/houses | 获取房源列表 |
| PATCH | /api/admin/houses/:id | 更新房源信息 |
| POST | /api/admin/houses/:id/publish | 发布/下架房源 |
| GET | /api/admin/orders | 获取订单列表 |
| POST | /api/admin/orders/:id/review | 审核订单 |
| GET | /api/admin/contracts | 获取合同列表 |
| PATCH | /api/admin/contracts/:id | 更新合同配置 |
| POST | /api/admin/contracts/:id/stamp | 合同盖章 |
| POST | /api/admin/contracts/:id/renew | 合同续签 |
| POST | /api/admin/contracts/:id/change-house | 合同换房 |
| POST | /api/admin/contracts/:id/terminate | 终止合同 |
| GET | /api/admin/bills | 获取账单列表 |
| POST | /api/admin/bills/import | 导入账单 |
| POST | /api/admin/bills/:id/verify-offline | 线下核销账单 |

---

## 9. 安全需求

### 9.1 认证与授权

- 管理员登录采用 JWT 认证，Token 有效期7天
- 支持基于角色的权限控制（系统管理员/店长）
- 店长只能访问所属门店的数据
- 敏感操作需二次确认

### 9.2 数据安全

- 密码采用 bcrypt 加密存储
- 敏感数据传输使用 HTTPS
- 数据库连接使用环境变量配置
- 定期备份数据库

### 9.3 审计日志

- 账单变更记录修改前后快照
- 记录操作者ID和操作时间
- 支持变更历史追溯

---

## 10. 部署与运维

### 10.1 环境要求

| 环境 | 配置 |
|-----|------|
| Node.js | 18+ |
| npm | 9+ |
| 内存 | 2GB+ |
| 磁盘 | 10GB+ |

### 10.2 部署步骤

```bash
# 1. 安装依赖
npm install

# 2. 配置环境变量
cp packages/server/.env.example packages/server/.env

# 3. 数据库迁移
npm run db:migrate

# 4. 初始化种子数据
npm run db:seed

# 5. 启动服务
npm run dev
```

### 10.3 服务地址

| 服务 | 地址 |
|-----|------|
| H5移动端 | http://localhost:5173 |
| 管理后台 | http://localhost:5174 |
| API服务 | http://localhost:8787 |

### 10.4 演示账号

| 角色 | 邮箱 | 密码 |
|-----|------|------|
| 系统管理员 | admin@example.com | admin123 |
| 店长 | manager@example.com | manager123 |

---

## 附录

### A. 术语表

| 术语 | 说明 |
|-----|------|
| CTHC | 产投华创房产（项目代号） |
| MVP | 最小可行产品 |
| PRD | 产品需求文档 |
| ORM | 对象关系映射 |
| JWT | JSON Web Token |

### B. 修订记录

| 版本 | 日期 | 修订内容 | 修订人 |
|-----|------|---------|-------|
| V1.0 | 2026-03-20 | 初始版本 | WorkBuddy |

---

**文档结束**
